node_modules/
.shogo/
dist/
*.db
*.db-shm
*.db-wal
memory/
.env
.jarvis-invite
.jarvis-keys.json
.jarvis-secret

# Workspace / pod-level files — must never be committed into an app repo.
# Committing these binds a new pod to the wrong pool and confuses the agent.
WORKSPACE.md
.shogo-pool-assignment
.shogo-runtime-version
